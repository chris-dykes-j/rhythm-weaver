package main


// For now, let's save the image to a directory. Long term, we may just want to generate them and send them to the client if it is possible.
// Weigh pros and cons later.
func makeImage(sequence []bool) {
    // dir := "./images/result"
    
}
